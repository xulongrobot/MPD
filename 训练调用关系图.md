# 模型训练调用关系图

## 完整调用流程

```mermaid
graph TD
    A[scripts/train/train.py<br/>experiment函数] -->|装饰器处理| B[@single_experiment_yaml]
    B -->|创建结果目录| C[create_results_dir]
    B -->|保存参数| D[save_args]
    B -->|启动日志| E[logging.basicConfig]
    B -->|WandB初始化| F[start_wandb]

    B -->|执行实验| G[experiment函数体]

    G -->|固定随机种子| H[fix_random_seed]
    G -->|获取设备| I[get_torch_device]

    G -->|加载数据| J[get_planning_task_and_dataset]
    J -->|加载环境| K[environments.EnvClass]
    J -->|加载机器人| L[robots.RobotClass]
    J -->|创建轨迹| M[ParametricTrajectoryBspline]
    J -->|加载数据集| N[TrajectoryDatasetBspline]
    J -->|创建DataLoader| O[DataLoader]

    G -->|构建上下文模型| P[ContextModelQs]
    G -->|构建上下文模型| Q[ContextModelEEPoseGoal]
    G -->|组合上下文模型| R[ContextModelCombined]

    G -->|构建UNet| S[TemporalUnet]
    S -->|时间编码器| T[TimeEncoder]
    S -->|下采样层| U[ResidualTemporalBlock downs]
    S -->|上采样层| V[ResidualTemporalBlock ups]
    S -->|注意力层| W[SpatialTransformer]

    G -->|创建模型| X[get_model]
    X -->|@model_loader装饰器| Y[model_loader wrapper]
    Y -->|获取模型类| Z[getattr models.GaussianDiffusionModel]
    Z -->|实例化模型| AA[GaussianDiffusionModel.__init__]
    AA -->|包装UNet| AB[MyDataParallel denoise_fn]
    AA -->|设置上下文模型| AC[context_model]
    AA -->|计算Diffusion参数| AD[注册buffers: betas, alphas等]

    G -->|获取损失函数| AE[get_loss]
    AE -->|GaussianDiffusionLoss| AF[GaussianDiffusionLoss]

    G -->|获取总结函数| AG[get_summary]
    AG -->|SummaryTrajectoryGeneration| AH[SummaryTrajectoryGeneration]

    G -->|开始训练| AI[trainer.train]

    AI -->|初始化EMA| AJ[EMA.__init__]
    AI -->|创建优化器| AK[torch.optim.AdamW]
    AI -->|创建混合精度| AL[GradScaler]
    AI -->|创建早停器| AM[EarlyStopper]
    AI -->|保存初始模型| AN[save_models_to_disk]

    AI -->|训练循环开始| AO[for epoch in epochs]
    AO -->|设置训练模式| AP[model.train]
    AO -->|遍历批次| AQ[for batch in train_dataloader]

    AQ -->|数据移到设备| AR[dict_to_device]
    AQ -->|计算损失| AS[loss_fn model, batch_dict, dataset]

    AS -->|GaussianDiffusionLoss.loss_fn| AT[GaussianDiffusionLoss.loss_fn]
    AT -->|提取归一化控制点| AU[input_dict.control_points_normalized]
    AT -->|调用模型损失| AV[diffusion_model.loss]

    AV -->|模型损失计算| AW[GaussianDiffusionModel.loss]
    AW -->|采样时间步| AX[torch.randint n_diffusion_steps]
    AW -->|前向传播| AY[p_losses]

    AY -->|添加噪声| AZ[q_sample x_start, t, noise]
    AZ -->|应用硬约束| BA[apply_hard_conditioning]
    AY -->|上下文编码| BB[context_model context_d]
    BB -->|ContextModelCombined.forward| BC[组合Qs和EE Pose]
    BC -->|ContextModelQs.forward| BD[MLP编码关节状态]
    BC -->|ContextModelEEPoseGoal.forward| BE[MLP编码末端执行器]

    AY -->|去噪预测| BF[model x_noisy, t, context_emb]
    BF -->|MyDataParallel forward| BG[DataParallel转发]
    BG -->|TemporalUnet.forward| BH[TemporalUnet前向传播]
    BH -->|时间编码| BI[time_mlp t]
    BH -->|下采样路径| BJ[downs ResidualTemporalBlock]
    BH -->|上采样路径| BK[ups ResidualTemporalBlock]
    BH -->|注意力机制| BL[SpatialTransformer]

    AY -->|应用硬约束| BM[apply_hard_conditioning x_recon]
    AY -->|计算损失| BN[loss_fn x_recon vs noise/x_start]
    BN -->|L2损失| BO[Losses l2]

    AV -->|返回损失| BP[loss, info]
    AT -->|返回损失字典| BQ[loss_dict, info]
    AS -->|聚合损失| BR[train_loss_batch.sum]

    AQ -->|每N步总结| BS{train_steps_current % steps_til_summary == 0?}
    BS -->|是| BT[do_summary]
    BT -->|设置评估模式| BU[model.eval]
    BT -->|调用总结函数| BV[summary_fn]
    BV -->|生成可视化| BW[生成轨迹图像/视频]
    BT -->|恢复训练模式| BX[model.train]

    BS -->|验证| BY{val_dataloader != None?}
    BY -->|是| BZ[遍历验证集]
    BZ -->|验证损失| CA[loss_fn model, val_batch]
    BZ -->|验证总结| CB[do_summary val]

    AQ -->|优化| CC[优化步骤]
    CC -->|清零梯度| CD[optim.zero_grad]
    CC -->|反向传播| CE[scaler.scale loss.backward]
    CE -->|梯度计算| CF[自动微分计算梯度]
    CF -->|TemporalUnet反向| CG[UNet各层反向传播]
    CF -->|ContextModel反向| CH[上下文模型反向传播]

    CC -->|梯度裁剪| CI{clip_grad?}
    CI -->|是| CJ[torch.nn.utils.clip_grad_norm_]

    CC -->|优化器步进| CK[scaler.step optim]
    CC -->|更新缩放器| CL[scaler.update]

    CC -->|更新EMA| CM{train_steps_current % update_ema_every == 0?}
    CM -->|是| CN[ema.update_model_average]
    CN -->|更新EMA参数| CO[ema_params = beta*old + 1-beta*new]

    AQ -->|保存检查点| CP{train_steps_current % steps_til_checkpoint == 0?}
    CP -->|是| CQ[save_models_to_disk]
    CQ -->|保存模型状态| CR[torch.save model.state_dict]
    CQ -->|保存完整模型| CS[torch.save model]
    CQ -->|保存损失| CT[save_losses_to_disk]

    AQ -->|早停检查| CU[early_stopper.early_stop]
    CU -->|停止训练| CV{stop_training?}

    AQ -->|更新步数| CW[train_steps_current += 1]
    CW -->|检查完成| CX{train_steps_current >= num_train_steps?}
    CX -->|否| AQ
    CX -->|是| CY[训练结束]

    CY -->|最终更新EMA| CZ[更新EMA模型]
    CY -->|保存最终模型| DA[save_models_to_disk]
    CY -->|保存最终损失| DB[save_losses_to_disk]
```

## 关键模块说明

### 1. 入口和装饰器层
- **`@single_experiment_yaml`**: 自动管理实验目录、参数保存、日志记录
- **`experiment`**: 主实验函数，包含所有配置参数

### 2. 数据加载层
- **`get_planning_task_and_dataset`**: 加载环境、机器人、数据集
- **`TrajectoryDatasetBspline`**: B-spline轨迹数据集
- **`DataLoader`**: PyTorch数据加载器

### 3. 模型构建层
- **`get_model`**: 模型工厂函数
- **`GaussianDiffusionModel`**: 扩散模型主类
- **`TemporalUnet`**: 时间UNet去噪网络
- **`ContextModel`**: 上下文条件编码器

### 4. 训练循环层
- **`trainer.train`**: 主训练循环
- **`loss_fn`**: 损失函数
- **`optimizer`**: 优化器（AdamW）
- **`EMA`**: 指数移动平均

### 5. 模型前向传播层
- **`model.loss`**: 计算训练损失
- **`model.p_losses`**: Diffusion前向扩散过程
- **`model.q_sample`**: 添加噪声
- **`TemporalUnet.forward`**: UNet前向传播
- **`ContextModel.forward`**: 上下文编码

### 6. 反向传播和优化层
- **`loss.backward`**: 自动微分
- **`clip_grad_norm_`**: 梯度裁剪
- **`optimizer.step`**: 参数更新
- **`EMA.update`**: EMA参数更新

### 7. 监控和保存层
- **`do_summary`**: 生成训练总结
- **`save_models_to_disk`**: 保存模型检查点
- **`save_losses_to_disk`**: 保存损失历史

## 数据流

```
数据集
  ↓
DataLoader (批次)
  ↓
dict_to_device (移到GPU)
  ↓
loss_fn
  ↓
diffusion_model.loss
  ↓
p_losses
  ├→ q_sample (添加噪声)
  ├→ context_model (编码上下文)
  └→ model.forward (UNet去噪)
  ↓
loss计算 (L2)
  ↓
loss.backward (反向传播)
  ↓
optimizer.step (更新参数)
  ↓
EMA.update (更新EMA)
```

## 关键时序

1. **初始化阶段**: 加载数据 → 构建模型 → 创建优化器
2. **训练阶段**: 迭代批次 → 前向传播 → 计算损失 → 反向传播 → 更新参数
3. **监控阶段**: 定期总结 → 验证 → 保存检查点
4. **完成阶段**: 最终保存 → 更新EMA → 清理资源
